program MaxElement;
var
  arr: array[1..5] of integer;
  i, max: integer;
begin
  arr[1] := 12;
  arr[2] := 45;
  arr[3] := 7;
  arr[4] := 89;
  arr[5] := 23;

  max := arr[1];

  for i := 2 to 5 do
    if arr[i] > max then
      max := arr[i];

  writeln('Maximum element: ', max);
end.
